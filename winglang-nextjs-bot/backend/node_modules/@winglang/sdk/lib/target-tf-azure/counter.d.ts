import { Construct } from "constructs";
import { StorageTable } from "../.gen/providers/azurerm/storage-table";
import * as cloud from "../cloud";
import * as core from "../core";
import { IInflightHost } from "../std";
/**
 * Azure bult-in storage account permissions.
 */
export declare enum StorageAccountPermissions {
    /** Read only permission */
    READ = "Storage Table Data Reader",
    /** Read write permission */
    READ_WRITE = "Storage Table Data Contributor"
}
/**
 * Azure implementation of `cloud.Counter`.
 *
 * @inflight `@winglang/sdk.cloud.ICounterClient`
 */
export declare class Counter extends cloud.Counter {
    private readonly storageAccount;
    readonly storageTable: StorageTable;
    constructor(scope: Construct, id: string, props?: cloud.CounterProps);
    /** @internal */
    get _liftMap(): core.LiftMap;
    onLift(host: IInflightHost, ops: string[]): void;
    /** @internal */
    _toInflight(): string;
    private envStorageAccountName;
    private envAccountKeyVariable;
    private envStorageTableName;
}
