import { RedisAttributes, RedisSchema } from "./schema-resources";
import { RedisClientBase } from "../ex";
import { ISimulatorContext, ISimulatorResourceInstance, UpdatePlan } from "../simulator/simulator";
export declare class Redis extends RedisClientBase implements ISimulatorResourceInstance {
    private readonly props;
    private connectionUrl?;
    private connection?;
    private isCleanedUp;
    constructor(props: RedisSchema);
    init(_context: ISimulatorContext): Promise<RedisAttributes>;
    cleanup(): Promise<void>;
    save(): Promise<void>;
    plan(): Promise<UpdatePlan>;
    rawClient(): Promise<any>;
    url(): Promise<string>;
}
