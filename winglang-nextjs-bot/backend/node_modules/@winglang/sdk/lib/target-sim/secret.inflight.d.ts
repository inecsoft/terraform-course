import { SecretAttributes, SecretSchema } from "./schema-resources";
import { ISecretClient } from "../cloud";
import { ISimulatorContext, ISimulatorResourceInstance, UpdatePlan } from "../simulator/simulator";
import { Json } from "../std";
export declare class Secret implements ISecretClient, ISimulatorResourceInstance {
    private _context;
    private readonly name;
    constructor(props: SecretSchema);
    private get context();
    init(context: ISimulatorContext): Promise<SecretAttributes>;
    cleanup(): Promise<void>;
    save(): Promise<void>;
    plan(): Promise<UpdatePlan>;
    value(): Promise<string>;
    valueJson(): Promise<Json>;
}
