import { DomainSchema } from "./schema-resources";
import { IDomainClient } from "../cloud";
import { ISimulatorContext, ISimulatorResourceInstance, UpdatePlan } from "../simulator";
export declare class Domain implements IDomainClient, ISimulatorResourceInstance {
    constructor(_props: DomainSchema);
    init(_context: ISimulatorContext): Promise<Record<string, any>>;
    cleanup(): Promise<void>;
    save(): Promise<void>;
    plan(): Promise<UpdatePlan>;
}
