import { TestRunnerAttributes, TestRunnerSchema } from "./schema-resources";
import { ISimulatorContext, ISimulatorResourceInstance, UpdatePlan } from "../simulator";
import { ITestRunnerClient, TestResult } from "../std";
export declare class TestRunner implements ITestRunnerClient, ISimulatorResourceInstance {
    private readonly tests;
    private _context;
    constructor(props: TestRunnerSchema);
    private get context();
    init(context: ISimulatorContext): Promise<TestRunnerAttributes>;
    cleanup(): Promise<void>;
    save(): Promise<void>;
    plan(): Promise<UpdatePlan>;
    listTests(): Promise<string[]>;
    runTest(path: string): Promise<TestResult>;
}
