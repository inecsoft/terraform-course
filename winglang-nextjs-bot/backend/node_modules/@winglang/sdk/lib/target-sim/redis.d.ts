import { Construct } from "constructs";
import { ISimulatorResource } from "./resource";
import { LiftMap } from "../core";
import * as ex from "../ex";
import { ToSimulatorOutput } from "../simulator";
import { IInflightHost } from "../std";
/**
 * Simulator implementation of `redis.Redis`.
 *
 * @inflight `@winglang/sdk.redis.IRedisClient`
 */
export declare class Redis extends ex.Redis implements ISimulatorResource {
    private readonly WING_REDIS_IMAGE;
    private readonly hostPort;
    constructor(scope: Construct, id: string);
    toSimulator(): ToSimulatorOutput;
    onLift(host: IInflightHost, ops: string[]): void;
    /** @internal */
    get _liftMap(): LiftMap;
    /** @internal */
    _toInflight(): string;
}
