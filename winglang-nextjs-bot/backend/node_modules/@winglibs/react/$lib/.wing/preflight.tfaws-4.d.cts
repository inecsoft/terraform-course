import * as $internal from "@winglang/sdk/lib/core/types"
import { std } from "@winglang/sdk"
import { cloud } from "@winglang/sdk"
import { fs } from "@winglang/sdk"
import { util } from "@winglang/sdk"
import * as api from "./preflight.api-2.cjs";
import * as utils from "./preflight.utils-1.cjs";
export class AppTfAws extends api.AppBase implements api.IApp
{
  constructor(scope: $internal.Construct, id: string, props: api.AppProps);
  [$internal.INFLIGHT_SYMBOL]?: AppTfAws$Inflight;
  getUrl: () => string;
  _preSynthesize: () => void;
}
export class AppTfAws$Inflight extends api.AppBase$Inflight implements api.IApp$Inflight
{
  constructor(props: api.AppProps);
}