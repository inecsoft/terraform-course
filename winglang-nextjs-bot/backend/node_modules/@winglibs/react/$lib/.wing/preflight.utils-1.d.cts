import * as $internal from "@winglang/sdk/lib/core/types"
import { std } from "@winglang/sdk"
export class Utils extends std.Resource
{
  constructor(scope: $internal.Construct, id: string);
  [$internal.INFLIGHT_SYMBOL]?: Utils$Inflight;
  static execSync: (command: string, env: Record<string, string>, cwd: string) => void;
}
export class Utils$Inflight
{
  constructor();
  static exec: (command: string, env: Record<string, string>, cwd: string) => Promise<() => Promise<void>>;
  static serveStaticFiles: (path: string, port: number) => Promise<() => Promise<void>>;
}