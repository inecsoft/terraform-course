import { Construct } from "constructs";
import * as std from "../std";
/**
 * Tf-Azure implementation of `cloud.TestRunner`.
 *
 * @inflight `@winglang/sdk.cloud.ITestRunnerClient`
 */
export declare class TestRunner extends std.TestRunner {
    constructor(scope: Construct, id: string, props?: std.TestRunnerProps);
    onLift(host: std.IInflightHost, ops: string[]): void;
    /** @internal */
    _preSynthesize(): void;
    private getTestFunctionIdentifiers;
    /** @internal */
    _toInflight(): string;
    private envTestFunctionIdentifiers;
}
