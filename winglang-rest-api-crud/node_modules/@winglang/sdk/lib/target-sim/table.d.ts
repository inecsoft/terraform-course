import { Construct } from "constructs";
import { ISimulatorResource } from "./resource";
import { LiftMap } from "../core";
import * as ex from "../ex";
import { ToSimulatorOutput } from "../simulator/simulator";
import { Json, IInflightHost } from "../std";
/**
 * Simulator implementation of `ex.Table`.
 *
 * @inflight `@winglang/sdk.ex.ITableClient`
 */
export declare class Table extends ex.Table implements ISimulatorResource {
    private readonly initialRows;
    constructor(scope: Construct, id: string, props?: ex.TableProps);
    addRow(key: string, row: Json): void;
    toSimulator(): ToSimulatorOutput;
    /** @internal */
    get _liftMap(): LiftMap;
    onLift(host: IInflightHost, ops: string[]): void;
    /** @internal */
    _toInflight(): string;
}
