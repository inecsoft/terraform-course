import { IPolicyClient } from "./policy";
import { PolicySchema } from "./schema-resources";
import { ISimulatorContext, ISimulatorResourceInstance, UpdatePlan } from "../simulator";
export declare class Policy implements IPolicyClient, ISimulatorResourceInstance {
    constructor(_props: PolicySchema);
    init(_context: ISimulatorContext): Promise<Record<string, any>>;
    cleanup(): Promise<void>;
    save(): Promise<void>;
    plan(): Promise<UpdatePlan>;
}
