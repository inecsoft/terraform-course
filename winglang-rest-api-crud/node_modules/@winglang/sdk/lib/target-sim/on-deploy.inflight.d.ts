import { OnDeployAttributes, OnDeploySchema } from "./schema-resources";
import { IOnDeployClient } from "../cloud";
import { ISimulatorContext, ISimulatorResourceInstance, UpdatePlan } from "../simulator/simulator";
export declare class OnDeploy implements IOnDeployClient, ISimulatorResourceInstance {
    private functionHandle;
    constructor(props: OnDeploySchema);
    init(context: ISimulatorContext): Promise<OnDeployAttributes>;
    cleanup(): Promise<void>;
    save(): Promise<void>;
    plan(): Promise<UpdatePlan>;
}
