import { Construct } from "constructs";
import { ISimulatorResource } from "./resource";
import { ToSimulatorOutput } from "../simulator/simulator";
import * as std from "../std";
import { IInflightHost } from "../std";
/**
 * Simulator implementation of `cloud.TestRunner`.
 *
 * @inflight `@winglang/sdk.cloud.ITestRunnerClient`
 */
export declare class TestRunner extends std.TestRunner implements ISimulatorResource {
    constructor(scope: Construct, id: string, props?: std.TestRunnerProps);
    toSimulator(): ToSimulatorOutput;
    onLift(host: IInflightHost, ops: string[]): void;
    /** @internal */
    _preSynthesize(): void;
    private getTestFunctionHandles;
    /** @internal */
    _toInflight(): string;
}
