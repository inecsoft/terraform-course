import { EventMappingAttributes, EventMappingSchema } from "./schema-resources";
import { ISimulatorContext } from "../simulator";
import { ISimulatorResourceInstance, UpdatePlan } from "../simulator/simulator";
export declare class EventMapping implements ISimulatorResourceInstance {
    private readonly publisher;
    private readonly subscriber;
    private readonly eventSubscription;
    private _context;
    constructor(props: EventMappingSchema);
    private get context();
    init(context: ISimulatorContext): Promise<EventMappingAttributes>;
    cleanup(): Promise<void>;
    save(): Promise<void>;
    plan(): Promise<UpdatePlan>;
}
