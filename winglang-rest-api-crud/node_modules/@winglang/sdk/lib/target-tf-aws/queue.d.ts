import { Construct } from "constructs";
import * as cloud from "../cloud";
import * as core from "../core";
import { IAwsQueue } from "../shared-aws";
import { IInflightHost } from "../std";
/**
 * AWS implementation of `cloud.Queue`.
 *
 * @inflight `@winglang/sdk.aws.IAwsQueueClient`
 */
export declare class Queue extends cloud.Queue implements IAwsQueue {
    private readonly queue;
    constructor(scope: Construct, id: string, props?: cloud.QueueProps);
    /** @internal */
    get _liftMap(): core.LiftMap;
    setConsumer(inflight: cloud.IQueueSetConsumerHandler, props?: cloud.QueueSetConsumerOptions): cloud.Function;
    onLift(host: IInflightHost, ops: string[]): void;
    /** @internal */
    _toInflight(): string;
    private envName;
    get queueArn(): string;
    get queueName(): string;
    get queueUrl(): string;
}
