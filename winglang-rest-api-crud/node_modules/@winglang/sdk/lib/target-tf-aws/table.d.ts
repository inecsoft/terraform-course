import { Construct } from "constructs";
import * as core from "../core";
import * as ex from "../ex";
import { IAwsTable } from "../shared-aws/table";
import { Json, IInflightHost } from "../std";
/**
 * AWS implementation of `ex.Table`.
 *
 * @inflight `@winglang/sdk.ex.ITableClient`
 */
export declare class Table extends ex.Table implements IAwsTable {
    private readonly table;
    constructor(scope: Construct, id: string, props?: ex.TableProps);
    addRow(key: string, row: Json): void;
    onLift(host: IInflightHost, ops: string[]): void;
    /** @internal */
    _toInflight(): string;
    /** @internal */
    get _liftMap(): core.LiftMap;
    private envName;
    private primaryKeyEnvName;
    private columnsEnvName;
    get dynamoTableArn(): string;
    get dynamoTableName(): string;
}
