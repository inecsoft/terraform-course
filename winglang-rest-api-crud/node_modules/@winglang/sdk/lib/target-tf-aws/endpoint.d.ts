import { Construct } from "constructs";
import * as cloud from "../cloud";
import { IInflightHost } from "../std";
/**
 * AWS implementation of `cloud.Endpoint`.
 */
export declare class Endpoint extends cloud.Endpoint {
    constructor(scope: Construct, id: string, url: string, props?: cloud.EndpointProps);
    /** @internal */
    onLift(host: IInflightHost, ops: string[]): void;
    /** @internal */
    _toInflight(): string;
    private urlEnvName;
}
