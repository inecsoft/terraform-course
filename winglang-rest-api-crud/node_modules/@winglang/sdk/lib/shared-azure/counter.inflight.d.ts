import type { ICounterClient } from "../cloud";
export declare class CounterClient implements ICounterClient {
    private readonly storageAccountName;
    private readonly storageTableName;
    private readonly accountKeyVariable;
    private readonly initial;
    private client;
    constructor(storageAccountName: string, storageTableName: string, accountKeyVariable: string, initial?: number);
    inc(amount?: number, key?: string): Promise<number>;
    dec(amount?: number, key?: string): Promise<number>;
    peek(key?: string): Promise<number>;
    set(value: number, key?: string): Promise<void>;
    private _getEntity;
    private _upsertEntity;
}
