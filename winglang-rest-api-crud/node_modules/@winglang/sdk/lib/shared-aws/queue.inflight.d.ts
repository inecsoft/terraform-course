import { SQSClient } from "@aws-sdk/client-sqs";
import { IAwsQueueClient } from "./queue";
export declare class QueueClient implements IAwsQueueClient {
    private readonly _queueUrlOrArn;
    private readonly client;
    private _queueUrl?;
    constructor(_queueUrlOrArn: string, client?: SQSClient);
    push(...messages: string[]): Promise<void>;
    queueUrl(): Promise<string>;
    purge(): Promise<void>;
    approxSize(): Promise<number>;
    pop(): Promise<string | undefined>;
}
