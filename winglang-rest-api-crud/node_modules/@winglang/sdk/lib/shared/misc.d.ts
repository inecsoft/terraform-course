/// <reference types="node" />
import { ExecOptions, ExecFileOptions } from "child_process";
export declare function readJsonSync(file: string): any;
/**
 * Normalize windows paths to be posix-like.
 */
export declare function normalPath(path: string): string;
/**
 * Just a helpful wrapper around `execFile` that returns a promise.
 */
export declare function runCommand(cmd: string, args: string[], options?: ExecFileOptions): Promise<any>;
/**
 * Just a helpful wrapper around `exec` that returns a promise.
 * This will run commands through the shell, while `runCommand` doesn't.
 */
export declare function shell(cmd: string, args: string[], options?: ExecOptions): Promise<any>;
export declare function isPath(s: string): boolean;
