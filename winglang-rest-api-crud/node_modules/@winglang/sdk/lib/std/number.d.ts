import { Json, JsonValidationOptions } from "./json";
/**
 * Number
 * @wingType num
 */
export declare class Number {
    /**
     * Parse a number from Json.
     *
     * @param json to parse number from.
     * @returns a number.
     */
    static fromJson(json: Json, options?: JsonValidationOptions): number;
    /**
     * Parse a number from string.
     *
     * @macro ((args) => { if (isNaN(args)) {throw new Error("unable to parse \"" + args + "\" as a number")}; return Number(args) })($args$)
     *
     * @param str to parse number from.
     * @returns a number.
     */
    static fromStr(str: string): number;
    /**
     * @internal
     */
    static _toInflightType(): string;
    private constructor();
}
