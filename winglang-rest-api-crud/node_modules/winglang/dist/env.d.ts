export interface EnvLoadOptions {
    /**
     * The directory to load the environment variables from.
     * @default process.cwd()
     */
    cwd?: string;
}
/**
 * Loads environment variables from `.env` and `.env.local` files.
 */
export declare function loadEnvVariables(options?: EnvLoadOptions): Record<string, string> | undefined;
