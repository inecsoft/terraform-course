import { Collector } from "./collector";
export interface OSData {
    arch: string;
    platform: string;
    release: string;
}
export declare class OSCollector extends Collector {
    collect(): Promise<OSData>;
}
