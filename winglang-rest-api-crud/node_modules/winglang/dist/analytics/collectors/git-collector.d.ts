import { Collector } from "./collector";
export interface GitData {
    anonymous_repo_id: string;
}
export interface GitCollectorProps {
    appEntrypoint?: string;
}
export declare class GitCollector extends Collector {
    private dir;
    constructor(props?: GitCollectorProps);
    collect(): Promise<GitData | undefined>;
    isInGitRepo(): Promise<boolean>;
    getRemoteUrl(): Promise<string | undefined>;
}
