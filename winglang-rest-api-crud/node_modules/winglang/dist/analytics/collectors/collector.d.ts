export interface Data {
    [key: string]: any;
}
export declare abstract class Collector {
    abstract collect(): Promise<Data | undefined>;
    protected runCommand(cmd: string, args: string[], dir?: string): Promise<any>;
}
