import { std } from "@winglang/sdk";
import { SingleTestResult } from "./test";
export declare function printResults(testResults: SingleTestResult[], duration: number): void;
export interface TestResultsJson {
    duration: number;
    platforms: string[];
    results: Record<string, Record<string, std.TestResult>>;
}
export declare function writeResultsToFile(testResults: {
    testName: string;
    results: std.TestResult[];
}[], duration: number, filePath: string, platforms: string[]): Promise<void>;
export declare function validateOutputFilePath(filePath: string): void;
