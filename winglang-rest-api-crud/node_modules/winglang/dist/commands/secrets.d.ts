import { CompileOptions } from "./compile";
export interface SecretsOptions extends CompileOptions {
    readonly list?: boolean;
}
export declare function secrets(entrypoint?: string, options?: SecretsOptions): Promise<void>;
