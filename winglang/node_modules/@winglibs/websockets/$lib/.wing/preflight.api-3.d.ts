import * as $internal from "@winglang/sdk/lib/core/types"
import { std } from "@winglang/sdk"
import { cloud } from "@winglang/sdk"
import * as api from "./preflight.api-1";
export interface WebSocketAwsRequestContext {
  readonly routeKey: string;
  readonly connectionId: string;
}
export interface WebSocketAwsRequest {
  readonly requestContext: WebSocketAwsRequestContext;
  readonly body: string;
}
export interface WebSocketAwsResponse {
  readonly statusCode: number;
  readonly body?: (string) | undefined;
}
export interface IAwsWebSocket extends api.IWebSocket
{
  readonly addRoute: (handler: cloud.Function, routeKey: string) => void;
}
export interface IAwsWebSocket$Inflight extends api.IWebSocket$Inflight
{
}