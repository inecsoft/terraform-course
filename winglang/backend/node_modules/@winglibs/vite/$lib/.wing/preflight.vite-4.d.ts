import * as $internal from "@winglang/sdk/lib/core/types"
import { std } from "@winglang/sdk"
import { cloud } from "@winglang/sdk"
import { sim } from "@winglang/sdk"
import { util } from "@winglang/sdk"
import { fs } from "@winglang/sdk"
import { ui } from "@winglang/sdk"
import * as vite_types from "./preflight.vitetypes-1";
import * as vite_sim from "./preflight.vitesim-2";
import * as vite_tf_aws from "./preflight.vitetfaws-3";
export class Vite extends std.Resource
{
  constructor(scope: $internal.Construct, id: string, props: vite_types.ViteProps);
  [$internal.INFLIGHT_SYMBOL]?: Vite$Inflight;
  _supportedOps(): $internal.OperationsOf<Vite$Inflight>;
  url: string;
}
export class Vite$Inflight
{
  constructor(props: vite_types.ViteProps);
}