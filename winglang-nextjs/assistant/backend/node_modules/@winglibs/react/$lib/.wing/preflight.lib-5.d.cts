import * as $internal from "@winglang/sdk/lib/core/types"
import { std } from "@winglang/sdk"
import { cloud } from "@winglang/sdk"
import { ui } from "@winglang/sdk"
import { util } from "@winglang/sdk"
import * as appApi from "./preflight.api-2.cjs";
import * as appSim from "./preflight.sim-3.cjs";
import * as appTfAws from "./preflight.tfaws-4.cjs";
export class App extends std.Resource
{
  constructor(scope: $internal.Construct, id: string, props: appApi.AppProps);
  [$internal.INFLIGHT_SYMBOL]?: App$Inflight;
  url: string;
  addEnvironment: (key: string, value: string) => void;
}
export class App$Inflight
{
  constructor(props: appApi.AppProps);
}